buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.0' // Plugin for Android
        classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:1.8.0' // Plugin for Kotlin
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
